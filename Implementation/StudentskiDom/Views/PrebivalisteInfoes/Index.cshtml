@model IEnumerable<StudentskiDom.Models.PrebivalisteInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kanton)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opcina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kanton)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opcina)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrebivalisteInfoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrebivalisteInfoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrebivalisteInfoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
